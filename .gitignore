# Secrets / environment (HECH QACHON git'ga tushmasin)
.env
*.env
!.env.example

# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
*.user

# Visual Studio
.vs/

# Node / client
node_modules/
dist/

# Uploads (runtime data)
IntellectCRM.Server/uploads/

# Local secrets
appsettings.*.local.json

# Server statik (Docker build dist'dan yaratadi)
IntellectCRM.Server/wwwroot/
# Andozalar (server-side HTML shablonlar) - gitda saqlanadi
!IntellectCRM.Server/wwwroot/
!IntellectCRM.Server/wwwroot/templates/
!IntellectCRM.Server/wwwroot/templates/**

# Dev artefaktlari (kurriкulum import manbasi + dizayn maketi) — serverga kerak emas
english.xlsx
matematika.xlsx
modul.png

# Lokal docker override (faqat shu mashinada — serverga tushmaydi)
docker-compose.override.yml

# Audit/review hisobot fayllari (generatsiya qilingan — repoga kerak emas)
*AUDIT*.md
*AUDIT*.txt
*_AUDIT*.md

# Lokal ish hujjatlari — mashinada qoladi, git'ga tushmaydi
CLAUDE.md
WORKLOG.md
DEPLOY.md
IntellectCRM_TZ.md

# Asterisk manba arxivi va lokal yozuvlar (asterisk-local) - git'da saqlanmaydi
asterisk-local/*.tar.gz
asterisk-local/recordings/

# Lokal PWA hujjatlari (gitga kirmaydi)
student_pwa.md
teacher_pwa.md

# Backup off-site sozlamasi (rclone.conf — bulut kalitlari, hech qachon commit qilinmaydi)
backup-config/

# Baza backuplari (haqiqiy foydalanuvchi maʼlumotlari) — commit qilinmasin
backups/
